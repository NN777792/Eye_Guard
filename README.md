### ✨ 核心特点

* **纯原生防蓝光**：底层调用 Win32 分层透明窗口，支持 5 档色温平滑调节；全屏无感点击穿透，运行完全不卡顿、不掉帧。
* **经典豆沙绿**：一键切换系统底层护眼色（`#C7EDCC`），大段阅读文字或浏览传统文档时柔和不刺眼。
* **极致轻量**：基于 **Tauri 2.0 + Rust** 构建，内存占用极低，支持系统托盘静默运行。![Uploading 图标.png…]()
